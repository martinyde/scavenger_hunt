{% extends 'base.html.twig' %}

{% block title %}ScavengerHunt{% endblock %}

{% block body %}
    <h1>ScavengerHunt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ scavenger_hunt.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ scavenger_hunt.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_scavenger_hunt_index') }}">back to list</a>

    <a href="{{ path('app_scavenger_hunt_edit', {id: scavenger_hunt.id}) }}">edit</a>

    {{ include('scavenger_hunt/_delete_form.html.twig') }}
{% endblock %}
