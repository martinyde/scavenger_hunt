{% extends 'base.html.twig' %}

{% block title %}ScavangerHunt{% endblock %}

{% block body %}
    <h1>ScavangerHunt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ scavanger_hunt.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ scavanger_hunt.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_scavanger_hunt_index') }}">back to list</a>

    <a href="{{ path('app_scavanger_hunt_edit', {'id': scavanger_hunt.id}) }}">edit</a>

    {{ include('scavanger_hunt/_delete_form.html.twig') }}
{% endblock %}
