<form method="post" action="{{ path('app_scavanger_hunt_delete', {'id': scavanger_hunt.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scavanger_hunt.id) }}">
    <button class="btn">Delete</button>
</form>
