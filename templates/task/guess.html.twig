{% extends 'base.html.twig' %}

{% block title %}Edit Task{% endblock %}

{% block body %}
    <h1>Resolve</h1>
  <div>
    {{ task.getTextBefore }}
  </div>
    <div>
      <img src="">
    </div>
    <div>
      {{ task.getTextAfter }}
    </div>
    <div>
      {{ form_start(guessForm) }}
      {{ form_row(guessForm.guess) }}
      <button type="submit" class="btn btn-primary">Guess</button>
      {{ form_end(guessForm) }}
    </div>


    <a href="{{ path('app_race_show', {'id': race.id, 'uuid': race.uuid}) }}">back to list</a>
{% endblock %}
