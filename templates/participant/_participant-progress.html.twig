<td>{{ participant.name }}</td>
<td>
  <div id="participant-progress--bar-{{ participant.id }}">
    {{ include('participant/_participant-progress-bar.html.twig', {'participant': participant}) }}
  </div>
</td>
